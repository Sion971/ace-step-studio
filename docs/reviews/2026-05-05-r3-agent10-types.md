# Type alignment R3 — verification of fix batch 4 — 2026-05-05 (agent10 round 3)

Scope: verify each R2 status against fix batch 4 commit `e53909eed` (HEAD). Re-hunt regressions. Build check.

Severity tags: **CRIT** · **MAJ** · **MIN** · **NIT** · **OK** · **DEFERRED** · **VERIFIED-FIXED** · **NEW-MIN**.

Counts: VERIFIED-FIXED 2 · DEFERRED 6 · OK 2 · NEW-MIN 1 · NEW-NIT 1.

---

## R3.§1 — R2.§5 / R1 §5 (`_tempId` via `as any`) — _previously MISLEADING-COMMIT_

**Status: VERIFIED-FIXED in `app/services/api.ts`.** Commit `e53909eed` adds `_tempId?: string` at `app/services/api.ts:394`. This is the type-add that commit `2419f7d73` falsely claimed.

**However, the cast in `App.tsx:1179` is NOT redundant** because `App.tsx:14` imports `GenerationParams` from `./types`, NOT from `./services/api`. `app/types.ts:GenerationParams` (lines 59-152) was NOT updated — it still lacks `_tempId`. So `(params as any)._tempId` at App.tsx:1009 and 1179 remains required for compilation. See R3.§4 below.

Verdict: api.ts type-add: **VERIFIED-FIXED**. Cast removal: **still blocked** by separate `types.ts` declaration.

Refs: `app/services/api.ts:394`, `app/types.ts:59-152`, `app/App.tsx:14,1009,1179`.

---

## R3.§2 — R2.§2 + R1 §2 (15 missing whitelist fields, backend wire-through) — _previously DEFERRED at backend_

**Status: VERIFIED-FIXED end-to-end.** Commit `e53909eed` adds the 14 fields (DCW cluster + FlowEdit cluster + retake + loraLoaded) to all three required spots in `app/server/src/routes/generate.ts`:

- **GenerateBody interface** (lines 222-236): `dcwEnabled`, `dcwMode`, `dcwScaler`, `dcwHighScaler`, `dcwWavelet`, `retakeSeed`, `retakeVariance`, `flowEditMorph`, `flowEditSourceCaption`, `flowEditSourceLyrics`, `flowEditNMin`, `flowEditNMax`, `flowEditNAvg`, `loraLoaded`.
- **Destructure block** (lines 393-407): all 14 pulled out of `req.body`.
- **`params` object** (lines 494-508): all 14 mirrored into the persisted blob.

Downstream consumer already exists:
- `app/server/src/services/acestep.ts:209` reads `params.dcwEnabled`.
- `acestep.ts:244` reads `params.retakeVariance`.
- `acestep.ts:247` reads `params.flowEditMorph`.

So the data path is now: `CreatePanel` IIFE → `App.tsx` whitelist → `startGeneration` → `routes/generate.ts` `req.body` destructure → `params` object → `JSON.stringify(params)` into `generation_jobs.params` column → `generateMusicViaAPI(params)` (line 528) → `acestep.ts` snake-case mapping → ACE-Step Python pipeline.

User-controllable knobs now ACTUALLY influence audio gen. Reuse-as-template restoration also works because the persisted blob mirrors what was submitted.

Verdict: **VERIFIED-FIXED end-to-end.** Resolves R2's "downgraded to silently dropped at backend layer" concern.

Refs: `app/server/src/routes/generate.ts:222-236,393-407,494-508,528`, `app/server/src/services/acestep.ts:209,244,247,378-389`.

---

## R3.§3 — Build / typecheck

- `cd app && npm run build` → **passes** in 2.10 s. 2350 modules. No errors. (Vite warns on >500 kB chunks — pre-existing.)
- `cd app && npx tsc --noEmit` → **6 errors, all pre-existing** (snake_case Song fields). Lines shifted from 877-882 (R2) to 887-892 (R3) because api.ts gained 24 new lines. Same 6 messages. No new errors from batch 4.
- `cd app/server && npx tsc --noEmit` → **clean**. The 14-field additions to GenerateBody, destructure, and params object all compile.

Verdict: **OK.** Build clean, no new TS regressions.

---

## R3.§4 — `(params as any)` casts and the dual-`GenerationParams` situation · **NEW-MIN**

R2.§12a noted "the casts can be removed cosmetically once api.ts has _tempId". This turned out to be wrong. Both `app/App.tsx:14` and `app/components/CreatePanel.tsx:4` import `GenerationParams` from `./types` (the local re-declaration). `app/types.ts` was not updated in batch 4 — it still declares only the original 51 fields, missing all of: `dcwEnabled`, `dcwMode`, `dcwScaler`, `dcwHighScaler`, `dcwWavelet`, `retakeSeed`, `retakeVariance`, `flowEditMorph`, `flowEditSourceCaption`, `flowEditSourceLyrics`, `flowEditNMin`, `flowEditNMax`, `flowEditNAvg`, `loraLoaded`, `_tempId`.

Counts:
- `(params as any).field` casts in `app/App.tsx`: **19** (1 in `handleGenerate`, 18 in the whitelist).
- Outer `} as any, token)` cast on `App.tsx:1180`: still present.
- `app/services/api.ts:GenerationParams` is now the more complete type; `app/types.ts:GenerationParams` is the stale one consumed at all call sites.

So R2.§12a's mitigation suggestion stands and only got half-applied: the api.ts type now matches the wire, but App.tsx still compiles against types.ts. To delete the casts, one of:
1. Mirror the same 15 fields into `app/types.ts:GenerationParams`, OR
2. Switch App.tsx + CreatePanel imports from `./types` to `./services/api`, OR
3. Delete the duplicate `types.ts:GenerationParams` and re-export from api.ts.

Functionally fine — runtime behavior unchanged, no TS errors, all 14 fields really do reach acestep.ts now. Just cosmetic + type-safety blind spot widening.

Verdict: **NEW-MIN — partial fix, dual-type drift now even more pronounced.** Recommend (1) or (3) in next pass.

Refs: `app/types.ts:59-152`, `app/services/api.ts:279-394`, `app/App.tsx:14`, `app/components/CreatePanel.tsx:4`.

---

## R3.§5 — End-to-end mental simulation: bulk=1 click in custom mode with all advanced settings

User flow walk-through to verify R3 data preservation claim:

1. User toggles DCW high-band, FlowEdit on with caption+lyrics+morph+nmin/max/avg, retake variance 0.5, lora loaded.
2. CreatePanel `tempIdForThisJob` allocated; custom-mode IIFE (`CreatePanel.tsx:1627-1739`) builds payload with all 51 base fields + 14 new fields + `_tempId`. TS compiles (excess-property check bypassed by ternary widening on line 1740).
3. `onGenerate(payload)` → `App.tsx:handleGenerate(params)` extracts `preCreatedId = (params as any)._tempId` (line 1009).
4. Whitelist (lines 1062-1180) forwards every field including all 14 new ones via `(params as any).field`. Outer `} as any, token)` keeps the call site silent.
5. `generateApi.startGeneration(params, token)` → `JSON.stringify` → POST `/v1/generate`.
6. Backend `routes/generate.ts:393-407` destructures 14 new fields out of `req.body`. Empty/undefined fields stay `undefined`.
7. `params` object (lines 477-509) mirrors all 14. SQLite write at line 521-526 stores `JSON.stringify(params)` into `generation_jobs.params` TEXT column. Reuse-as-template can read this back.
8. `generateMusicViaAPI(params)` at line 528 hands the same object to `acestep.ts:requestParams` (line 198+). Snake-case mapping at 209, 244, 247 etc. converts to Python kwargs.
9. ACE-Step pipeline runs with user knobs applied. Audio actually different than default. ✅
10. Status poll → song row written with audio URL. UI renders song.
11. Reuse-as-template later: `params` JSON parsed and pushed back into CreatePanel state via `s.dcwEnabled` etc. (CreatePanel.tsx:429+). UI restores correctly.

End-to-end data preservation: **OK.** Every R2 concern resolved.

The only field that's still wire-noise is `prompt` (R2.§12c) — backend doesn't destructure it, persisted blob still gains a duplicate of `lyrics`/`songDescription`. Still DEFERRED.

Verdict: **OK.**

---

## R3.§6 — Other R1/R2 deferrals — status check

| Ref | R2 Status | R3 Status | Notes |
|---|---|---|---|
| §1 (3 GenerationParams types) | DEFERRED | **DEFERRED** + drift INCREASED — `api.ts` got 15 new fields, `types.ts` did not (R3.§4) |
| §3 (samplerMode 2-of-10 lie) | DEFERRED | **DEFERRED** — `api.ts:355` still `'euler' \| 'heun'`, App.tsx:1129 cast unchanged |
| §4 (repaintMode 'most_natural') | DEFERRED | **DEFERRED** |
| §3-MIN (openrouterModel nullability drift) | DEFERRED | **DEFERRED** |
| §4-MIN (Pollinations defaults double-applied) | DEFERRED | **DEFERRED** |
| §9 (GenerationJob.result missing fields) | DEFERRED | **DEFERRED** |
| §16 (two Song types) | DEFERRED | **DEFERRED** |
| §10 build check 6 errors | pre-existing | **pre-existing** (lines 887-892) |
| R2.§12a (`as any` count) | NEW-MIN | **NEW-MIN R3.§4** — partial, types.ts not patched |
| R2.§12b (mp3 forwarding asymmetry) | NEW-NIT | **DEFERRED** |
| R2.§12c (stale `prompt` forward) | NEW-MIN | **DEFERRED** |

---

## R3.§7 — Hooks-deps regression check on App.tsx batch-4 changes · **NIT**

Batch 4 added `drainQueueWaiters` to two `useCallback` deps lists:
- `cancelGeneration` (line 790).
- `resetSingleJob` (line 816).

`drainQueueWaiters` is itself `useCallback(..., [])` at line 98-103 → stable identity. Adding to deps causes no re-renders. ESLint react-hooks/exhaustive-deps would now be satisfied. No regression.

Cancel-flow correctness:
- `cancelGeneration` now removes job from `activeJobsRef`, decrements `activeJobCount`, flips `isGenerating` if zero, then drains queue waiters. Resolves the "permanent occupant" hang R2 agent05 NEW-HIGH-R4 flagged.
- `resetSingleJob` adds `drainQueueWaiters()` after the existing isGenerating flip — same family fix.

Verdict: **OK.**

---

## Summary table

| Ref | R3 Status |
|---|---|
| R1 §5 / R2.§5 — `_tempId` cast | api.ts type added: VERIFIED-FIXED. App.tsx cast: still required (R3.§4) |
| R1 §2 / R2.§2 — DCW/FlowEdit/retake/lora end-to-end | **VERIFIED-FIXED** (GenerateBody + destructure + params + acestep.ts consumer) |
| Build check | tsc clean (6 pre-existing snake_case errors), Vite build clean, server tsc clean |
| R3.§4 — dual-GenerationParams drift | **NEW-MIN** — api.ts updated, types.ts not |
| R3.§7 — cancel/reset hook-deps | OK |
| Other R1/R2 deferrals | unchanged |

## Verdict

Batch 4 lands the two R2 highest-priority items cleanly:
1. Backend now actually accepts + persists + forwards DCW/FlowEdit/retake/lora — user knobs really do influence audio gen and reuse-as-template now works.
2. The misleading-commit `_tempId` claim is now true: api.ts has the field.

One half-applied fix:
- `app/types.ts:GenerationParams` was not updated to match `app/services/api.ts:GenerationParams`. Since App.tsx and CreatePanel import from `./types`, the 19 `(params as any)` casts cannot be removed cosmetically. The type-safety blind spot R2.§12a flagged is unchanged in practice.

No new build/runtime regressions. Top three for next round (unchanged from R2):
1. Mirror the 15 fields into `app/types.ts:GenerationParams` (or unify by re-export) so the App.tsx casts can finally drop.
2. Drop the redundant `prompt:` whitelist forwarding (or wire it backend-side).
3. Address the deferred sampler/repaintMode/Song/openrouterModel drifts when ready for a heavier refactor.
